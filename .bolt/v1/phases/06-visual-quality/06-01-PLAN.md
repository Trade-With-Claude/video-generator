# Phase 6 Plan: Visual Quality Pass

## Acceptance Criteria

### AC1: Each preset uses a unique combination of effect layers
- **Given** three presets
- **When** I inspect the layer stacks
- **Then** each preset has a different set of layers (not just different colors on the same two layers)

### AC2: Renders look visually rich and distinct
- **Given** 10s renders of all three presets
- **When** I compare file sizes and inspect
- **Then** significant size differences and each has its own visual identity

## Tasks

### Task 1: New effect layers
- **files:** `src/video_generator/effects.py`
- **action:** Add Layer subclasses: GlowParticleLayer (soft particles + gaussian glow), AuroraLayer (sinusoidal color ribbons), FlowFieldLayer (noise-driven curved particle paths), VignetteLayer (radial edge darkening), ColorCycleBackground (rotating multi-color radial gradient).
- **verify:** `python3 -c "from video_generator.effects import GlowParticleLayer, AuroraLayer, FlowFieldLayer, VignetteLayer, ColorCycleBackground; print('all effects ok')"`
- **done:** true

### Task 2: Rewire presets + tune + render samples
- **files:** `src/video_generator/presets.py`, `src/video_generator/render.py`, `scripts/test_presets.py`
- **action:** Expand presets with layer config. Rewire build_layers() per preset: ambient (ColorCycle + Aurora + GlowParticles + Vignette), focus (Noise + FlowField + sharp particles + Vignette), trippy (ColorCycle fast + FlowField + Aurora + GlowParticles). Render 10s samples, tune params until they look good.
- **verify:** `python3 scripts/test_presets.py` produces three distinct MP4s
- **done:** true

## Boundaries
- **DO NOT CHANGE:** `time_loop.py`, `ffmpeg.py`, `loop.py`, `config.py`, `generate.py`, `tui.py`, `__main__.py`
